#ifndef __LAUNCH_H
#define __LAUNCH_H

#define MIN(a,b) ((a) > (b) ? (b) : (a))
#define MAX(a,b) ((a) > (b) ? (a) : (b))

#define BOOTTIMEOUT 2400
#define RESETTIMEOUT 250
#define MODE_INTERACTIVE        0
#define MODE_NONINTERACTIVE     1

#define PAD_CHECK_TIME     100
#define MAX_ARGS           3

typedef unsigned long ulong;
#endif
